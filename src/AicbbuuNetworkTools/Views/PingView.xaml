<UserControl x:Class="AicbbuuNetworkTools.Views.PingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AicbbuuNetworkTools.ViewModels"
             Background="Transparent">
    <Border Style="{StaticResource CardBorder}" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Ping 测试" Style="{StaticResource TitleText}" FontSize="18"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding Target, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                <Button Content="开始" Style="{StaticResource PrimaryButton}" Click="Start_Click" Grid.Column="1"/>
            </Grid>

            <Border Grid.Row="2" Background="#0E1322" CornerRadius="8" Padding="12" Margin="0,4,0,0">
                <TextBlock Text="{Binding Result}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
